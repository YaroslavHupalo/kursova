# 📋 Опис проекту "Бібліотека Військового Інституту"

## ✅ Виконані вимоги курсової роботи

### Технології Frontend:
- ✅ **HTML** - використовується в React компонентах
- ✅ **CSS** - базові стилі
- ✅ **SCSS** - препроцесор CSS (всі .scss файли)
- ✅ **JavaScript** - мова програмування
- ✅ **TypeScript** - типізація для всього проекту
- ✅ **React.js** - фреймворк для frontend

### Технології Backend:
- ✅ **Node.js** - серверне середовище
- ✅ **Express.js** - веб-фреймворк
- ✅ **TypeScript** - типізація backend
- ✅ **MongoDB Atlas** - хмарна база даних
- ✅ **Mongoose** - ODM для MongoDB
- ✅ **REST API** - взаємодія Frontend-Backend
- ✅ **JWT** - автентифікація

### Деплой:
- ✅ Готовність до хостингу на Vercel/Netlify (frontend)
- ✅ Готовність до хостингу на Render/Railway (backend)
- ✅ Git репозиторій ready

## 🎯 Реалізований функціонал

### 1. Авторизація
- Вхід бібліотекаря в систему
- JWT токени для захисту
- Protected routes
- Автоматичне перенаправлення

### 2. Головна панель (Dashboard)
- Карточки зі статистикою (всього, активні, повернуті, прострочені)
- Найближчі повернення (наступні 7 днів)
- Швидкі дії (кнопки навігації)
- Топ-5 популярних книг
- Адаптивний дизайн

### 3. Видача книги
**Форма з полями:**
- Прізвище, Ім'я, По батькові
- Звання (dropdown з варіантами)
- Підрозділ
- Назва книги
- Дата видачі (автоматично поточна)
- Дата повернення (з швидкими кнопками +14/+30 днів)
- Примітки (необов'язково)

**Функції:**
- ✅ Автоматична генерація унікального 8-значного ID
- ✅ Валідація всіх полів
- ✅ Успішне повідомлення з ID видачі
- ✅ Автоматичне перенаправлення після успіху

### 4. Активні видачі
- Таблиця з усіма активними видачами
- Пошук за ID, ПІБ, книгою, підрозділом
- Статуси (активна, прострочено, скоро термін)
- Підрахунок днів до повернення
- Кнопка "Повернути" для кожної видачі
- Червоне виділення прострочених
- Responsive table

### 5. Прострочені книги
- Список книг з простроченим терміном
- Кількість днів прострочення
- Попередження для бібліотекаря
- Можливість повернути книгу
- Сортування за датою

### 6. Історія
- Всі повернуті книги
- Пошук
- Статуси: вчасно / прострочено
- Порівняння планової та фактичної дати повернення
- Візуальне виділення прострочених повернень

### 7. Статистика
- Загальна статистика (4 картки)
- Топ-10 найпопулярніших книг з прогрес-барами
- Статистика по підрозділах з прогрес-барами
- Візуалізація даних
- Динамічне оновлення

## 🎨 UI/UX Features

### Дизайн:
- Сучасний gradient design (синій військовий колір)
- Адаптивна верстка (responsive)
- Мобільна версія
- Інтуїтивна навігація
- Іконки для зручності
- Hover ефекти
- Smooth transitions

### UX:
- Зручна навігація через sidebar
- Швидкі дії на головній
- Пошук в реальному часі
- Підказки та плейсхолдери
- Повідомлення про успіх/помилки
- Loading states
- Підтвердження важливих дій

## 📁 Структура файлів

### Backend (TypeScript)
```
backend/src/
├── config/
│   └── database.ts          # З'єднання з MongoDB Atlas
├── controllers/
│   ├── auth.controller.ts   # Логіка авторизації
│   └── bookIssue.controller.ts  # Логіка видач книг
├── middleware/
│   ├── auth.middleware.ts   # JWT перевірка
│   └── errorHandler.ts      # Обробка помилок
├── models/
│   ├── User.model.ts        # Mongoose схема користувача
│   └── BookIssue.model.ts   # Mongoose схема видачі
├── routes/
│   ├── auth.routes.ts       # Маршрути авторизації
│   └── bookIssue.routes.ts  # Маршрути видач
└── server.ts                # Головний файл сервера
```

### Frontend (React + TypeScript)
```
frontend/src/
├── components/
│   ├── Layout.tsx / .scss       # Головний layout з навігацією
│   └── ProtectedRoute.tsx       # Захист маршрутів
├── context/
│   └── AuthContext.tsx          # Context для авторизації
├── pages/
│   ├── Login.tsx / .scss        # Сторінка входу
│   ├── Dashboard.tsx / .scss    # Головна панель
│   ├── IssueBook.tsx / .scss    # Форма видачі книги
│   ├── ActiveIssues.tsx / .scss # Активні видачі
│   ├── OverdueIssues.tsx / .scss # Прострочені
│   ├── History.tsx / .scss      # Історія
│   └── Statistics.tsx / .scss   # Статистика
├── services/
│   ├── api.ts                   # Axios instance
│   ├── auth.service.ts          # API методи авторизації
│   └── bookIssue.service.ts     # API методи видач
├── App.tsx                      # Головний компонент
└── index.tsx                    # Entry point
```

## 🔧 Технічні особливості

### Безпека:
- JWT токени з expiration
- Bcrypt хешування паролів
- Protected API endpoints
- CORS налаштування
- Environment variables

### База даних:
- MongoDB Atlas (хмарна)
- Mongoose schemas з валідацією
- Індекси для швидкого пошуку
- Автоматичні timestamps

### API:
- RESTful архітектура
- JSON формат
- HTTP status codes
- Error handling
- Request validation

### Frontend:
- React Router v6
- Context API для стану
- Axios interceptors
- TypeScript інтерфейси
- SCSS модулі

## 📊 Моделі даних

### User (Бібліотекар)
```typescript
{
  username: string;
  password: string;  // hashed
  fullName: string;
  role: 'librarian';
  createdAt: Date;
  updatedAt: Date;
}
```

### BookIssue (Видача книги)
```typescript
{
  issueId: string;      // 8-значний унікальний
  lastName: string;
  firstName: string;
  middleName: string;
  rank: string;
  department: string;
  bookTitle: string;
  issueDate: Date;
  returnDate: Date;
  actualReturnDate?: Date;
  status: 'active' | 'returned' | 'overdue';
  notes?: string;
  createdBy: ObjectId;  // посилання на User
  createdAt: Date;
  updatedAt: Date;
}
```

## 🚀 Готовність до презентації

✅ Повністю робочий прототип  
✅ Красивий UI дизайн  
✅ Всі основні функції реалізовані  
✅ Адаптивна верстка  
✅ Готовність до деплою  
✅ Документація (README, QUICK_START)  
✅ Git ready  

## 📝 Що потрібно зробити перед презентацією:

1. ✅ Налаштувати MongoDB Atlas
2. ✅ Створити .env файл
3. ✅ Встановити залежності (npm install)
4. ✅ Запустити backend (npm run dev)
5. ✅ Створити першого користувача
6. ✅ Запустити frontend (npm start)
7. ✅ Створити 5-10 тестових видач
8. ✅ Протестувати всі функції
9. ⬜ Задеплоїти на хостинг (опціонально)
10. ⬜ Створити Git репозиторій

## 🎓 Відповідність вимогам

**Курсова робота повністю відповідає вимогам:**
- Використано HTML, CSS, SCSS, JavaScript, TypeScript
- Фреймворк React.js
- Серверні технології (Node.js, Express)
- База даних (MongoDB Atlas)
- REST API
- Готовність до хостингу
- Можливість перегляду на Git
